# CLAUDE.md — Webアクセスログ解析ツール 開発ルール

<meta>
audience: Claude Code(実装担当AIエージェント)
purpose: 本プロジェクトの実装時に常時遵守する運用ルールの唯一の定義
read_order: 本ファイル → 01_requirements.md → 02_basic_design.md → 03_detailed_design.md → 04_ui_design.md → SETUP.md
language: ユーザーとの対話・コメントは日本語、コード識別子は英語
enforcement: 本ファイルのルールはデフォルト動作より優先される。矛盾時は「STOP」して確認する
execution_mode: フェーズ分割(設計は確定済み。承認待ちは不要だが、1回の指示では1フェーズ分だけを実装し、テスト→コミット→pushで区切る。P1〜P6を一度に通しでやらない。第0.1章参照)
</meta>

本ファイルはAIが読んで従うことを前提に構造化している。各ルールはAIが判定・実行できる粒度で記述する。作業開始時に必ず本ファイルと設計書を読み込むこと。

## 0. 最重要原則(MUST / 絶対遵守)

- P0-1: 設計書(01/02/03/04)を唯一の「正」とする。そこに書かれた内容だけを実装する。
- P0-2: 設計書から読み取れることのみ実装する。推測・一般論・「よくある実装」「ベストプラクティス」を理由にした機能追加・仕様変更を勝手に行わない。
- P0-3: 設計書に書かれていないこと(仕様の穴・曖昧な点)に遭遇したら、直ちに実装を止める(STOP)。ただし本プロジェクトは設計を詳細まで確定済みで、未確定事項は無い(03 第9章)。通常この状況は発生しない想定。
- P0-4: 確認はエビデンス必須。「該当設計書の章・節」「不足/矛盾/曖昧の別」「選択肢と各影響」を提示し、ユーザーの決定を待つ(フォーマットは第3章)。
- P0-5: 確認せず推測で実装したものは成果物として認めない。設計書に本当に根拠が無いときのみSTOPして聞く。
- P0-6: 一度に雑に作らない。03の第7章の機能IDと第8章のフェーズ(P1→P6)の順に、フェーズごとに実装・テスト・コミットしながら進める。

## 0.1 実行モード(フェーズ分割・こまめに区切る)

本プロジェクトは設計(01/02/03/04)を詳細まで確定済みで、未確定事項は無い。承認待ちは不要だが、整合性を保つため、**1回の指示では1フェーズ分だけ**を実装する。P1〜P6を一度に通しで実装しない。

各フェーズで守ること:

- そのフェーズに含まれる機能IDだけを実装する。次フェーズを先取りしない。
- テスト必須ID(第6章)は必ずテストを書き、`mvn test` を通してから区切る。
- 機能IDごと(または意味のある区切りごと)にcommitし、フェーズ完了時にGitHubへpushする(第8章)。
- コードは第8.1章の可読性ルールを守る。
- フェーズ完了時に、実装した機能ID・テスト結果を日本語で報告し、そこでいったん止まる。次フェーズはユーザーの次の指示で始める。
- 設計書どうしが矛盾、または設計書に本当に記載が無い事項に限り、STOP(第3章)して確認する。

## 1. 正とする設計書(SSOT)

| 記号 | ファイル | 役割 |
|---|---|---|
| 01 | `01_requirements.md` | 要件定義(MVP範囲・段階実装) |
| 02 | `02_basic_design.md` | 基本設計(技術スタック・構成・画面・機能一覧) |
| 03 | `03_detailed_design.md` | 詳細設計(データモデル・パース仕様・集計・API・画面・機能ID・フェーズ・確定事項) |
| 04 | `04_ui_design.md` | 画面設計(配色・レイアウト・HTML構造・CSS。画面はこの通りに作る) |
| - | `SETUP.md` | 環境構築手順(JDK/IntelliJ/Spring Initializr) |

優先順位: 記載が新しいものを優先。設計書間で矛盾を見つけたら実装せずSTOPして確認する。未確定事項(03 第9章等)は着手前にユーザー確認が必要。

## 2. スコープ判断ルール(実装してよいか)

各実装アクションの前にチェックする。1つでもNoならSTOPして確認する。

- C2-1: その機能・項目・API・画面要素は設計書に明記されているか。
- C2-2: 設計書の内容を簡略化・省略・変更していないか。
- C2-3: 使用技術は 02 のスタック(Java 21 / Spring Boot / Maven / Thymeleaf / JUnit5)の範囲内か。範囲外ライブラリの導入は理由と代替案を添えて確認したか。
- C2-4: 「良かれと思った」リファクタ・機能追加・UI変更に設計書上の根拠があるか。

## 3. 不明点・仕様の穴への対応(STOP時の確認フォーマット)

判断できない点に当たったら、次を出力してユーザーの回答を待つ。回答を得るまで、その論点に依存する実装は進めない。

```
【確認事項】
- 論点: (何を決める必要があるか)
- 該当箇所: (設計書名 / 章・節。例: 03_detailed_design.md 第3章)
- 関連機能ID: (該当する F-XXX-NN。なければ「該当なし」)
- 状況: (なぜ設計書だけでは決められないか。不足/矛盾/曖昧の別)
- 選択肢: (考えられる案。それぞれのメリット・デメリット)
- 影響範囲: (この決定が及ぼす機能ID・クラス・画面)
```

回答が出たら、まず該当設計書へ反映(追記・修正)する提案を行い、承認後に実装へ進む。

## 4. 作業ワークフロー(1機能IDごとに繰り返す)

1. PLAN(計画提示): 対象機能ID・変更/追加ファイル・手順・参照する設計書の章節を提示。
2. APPROVE(承認): ユーザーの承認を得る。承認前に実装しない。
3. IMPLEMENT(実装): 承認範囲のみ実装。範囲外の気づきは実装せずメモし、別途確認。
4. VERIFY(検証): 第6章の方法で検証し、機能IDの完了条件を満たすか確認。
5. REPORT(報告): 満たした設計書要件と機能IDを報告し、レビューを受ける。

進行順は 03 第8章のフェーズ順(P1→P6)。前フェーズのDoD未達なら次へ進まない。

## 5. 段階実装とフェーズ管理

- 実装単位は 03 第7章の機能ID(`F-{分類}-{連番}`)。
- フェーズ(P1〜P6)ごとに開始条件(前フェーズ完了)と完了条件(DoD)がある。DoD達成まで次へ進まない。
- 各機能IDの状態を「未着手 / 実装中 / 実装済み(未検証) / 検証済み」で管理し、フェーズ着手時に一覧提示する。
- フェーズをまたぐ先取り実装はしない。必要が生じたらSTOPして確認する。

## 6. テスト・検証(VERIFY)

- 主要ロジックはテスト必須(JUnit 5)。少なくとも次を書く(03 8.1)。
  - F-PRS-01(1行パース) / F-PRS-02(スキップ) / F-PRS-03(日時)
  - F-AGG-02〜05(ステータス区分・上位・時間バケット)
- テストデータは `sample-logs/access.log` を利用してよい。
- テストの期待値は設計書の記述に基づく。曖昧で期待値が定まらない場合は、実装より先にSTOPして確認する。
- 検証時は実装が設計書の該当章節どおりかを突き合わせ、機能IDのDoDを満たすか確認する。

## 7. 言語・命名ルール

- 対話・コメント・ドキュメントは日本語。識別子(変数/メソッド/クラス/パッケージ)は英語。
- クラス名・フィールド名・enum値は 03 第1章・第2章の定義に従う(勝手な命名をしない)。
- コミットメッセージは日本語で、変更内容・対応機能ID・設計書箇所が分かるように書く(例: `F-PRS-01: ログ1行のパースを実装 (03 3章)`)。

## 8. Git運用(GitHubで版管理・こまめに)

- 意味のある区切り(原則1機能ID)ごとにcommitする。1コミットに無関係な変更を混ぜない。
- テストが通った区切りでcommitする。コミットメッセージは日本語で、対応機能ID・設計書箇所を含める。
- 各フェーズ(P2〜P6)の完了時、遅くともフェーズごとにGitHubへpushする。こまめなpushを基本とし、作業を長時間ローカルに溜めない。
- 1回のセッションで大量の変更を一気に行わない。原則「1フェーズ = 1まとまりの作業」とし、フェーズをまたぐ実装を先取りしない(整合性維持のため)。1フェーズが終わったら区切り、次フェーズは新たな指示で着手する。

## 8.1 コードの可読性(人手・AIによる改修を前提)

本コードは、後から人間または AI(リバースエンジニアリング的な読み取り含む)が改修することを前提とする。次を必ず守る。

- 公開クラス・公開メソッドには日本語のJavadocを付け、「何をするか」「引数」「戻り値」を明記する。
- メソッドは1つの責務に絞り、長くなりすぎたら private メソッドに分割する。ネストは浅く保つ。
- 変数・メソッド名は役割が分かる英語にする(例: `parseLine`, `topPaths`)。省略しすぎない。
- マジックナンバー・マジック文字列は名前付き定数にする(例: `MIN_TOP_N`)。意図をコード上に残す。
- 1つのクラス・メソッドが設計書のどの機能ID・章に対応するかを、クラス冒頭コメントに記す(例: `// 03 第3章 / F-PRS-01`)。
- 例外時の挙動(スキップする等)はコメントで理由を残す。黙って握りつぶさない。
- フォーマットは一貫させる(インデント・空行)。IDEの自動整形に従う。

## 9. プロジェクト構成・環境

- 環境構築は SETUP.md に従う(JDK21 / IntelliJ / Spring Initializr / Maven)。
- パッケージ構成は 03 第1章の案を基本とし、最終確定はP1で提案・承認する(未確定事項)。
- `target/`(ビルド成果物)等は `.gitignore` で除外する。秘匿情報はコミットしない。

---

補足: 本ファイルのルールと設計書(01/02/03)の仕様が矛盾する場合は、解釈で迷う可能性があるため、実装せずSTOPしてユーザーに確認する。
