# Webアクセスログ解析ツール 要件定義書

## 1. 目的・背景

SES(インフラ系)の新卒エンジニアが、Javaの実力(ロジック実装・テスト・設計・API構築)を示すためのポートフォリオとして、Webサーバー(Apache/Nginx風)のアクセスログを解析・集計するツールを作成する。インフラ運用で実際に行う「ログからの状況把握(アクセス状況・エラー傾向)」を題材とし、実用性と技術アピールを両立する。

## 2. 想定利用者・利用シーン

- 利用者: 本人(ポートフォリオ)、および閲覧する採用担当・現場担当
- 利用シーン: アクセスログをアップロードし、ステータスコード分布・アクセス上位・時間帯別傾向・エラー抽出を確認する

## 3. 技術スタック

| 区分 | 採用技術 |
|---|---|
| 言語 | Java(JDK 21 / LTS) |
| フレームワーク | Spring Boot |
| ビルドツール | Maven |
| 画面 | Thymeleaf(サーバー側でHTML生成) |
| テスト | JUnit 5 |
| IDE | IntelliJ IDEA Community |

環境構築の具体手順は `SETUP.md` を参照。

## 4. 対象ログ形式

Apache/Nginx の Combined Log Format を主対象とする。1行の例:

```
192.168.0.10 - - [30/Jul/2026:10:15:32 +0900] "GET /index.html HTTP/1.1" 200 1043 "https://example.com/" "Mozilla/5.0"
```

パースで取り出す項目:

- クライアントIP(例: 192.168.0.10)
- 日時(例: 30/Jul/2026:10:15:32 +0900)
- HTTPメソッド(GET/POST 等)
- リクエストパス(例: /index.html)
- プロトコル(HTTP/1.1)
- ステータスコード(例: 200)
- レスポンスサイズ(バイト。`-` の場合は0または未定義)
- リファラ、ユーザーエージェント(取得はするが、MVPの集計対象は任意)

形式に合わない行は「解析不能行」としてスキップ数をカウントする(処理は止めない)。

## 5. 機能要件(MVP)

### 5.1 ログの入力

- Web画面のテキストエリアにアクセスログを貼り付けて送信する(MVPではファイルアップロードは対象外)。
- あわせて、集計の「上位件数(Top N)」と「時間バケットの単位」を画面から指定できる。

### 5.2 解析・集計

入力ログをパースし、以下を集計する。

- 総リクエスト数、解析成功数、解析不能行数
- ステータスコード別の件数(2xx/3xx/4xx/5xx の区分と、主要コード個別)
- リクエストパスのアクセス上位(Top N)
- クライアントIPのアクセス上位(Top N)
- 時間帯別のリクエスト数(1時間ごとなどのバケット)
- エラー行(4xx / 5xx)の抽出一覧

### 5.3 結果表示

- 集計結果をWeb画面に表・数値で表示する。
- 時間帯別リクエスト数は簡易なグラフ(棒グラフ等)で表示できると望ましい(MVPでは表でも可)。

### 5.4 REST API

- 集計結果をJSONで返すREST APIエンドポイントを用意する(画面はこのAPIを利用してもよい)。
- 例: `POST /api/analyze`(ログを受け取り集計結果JSONを返す)。

## 6. 非機能要件

- ローカル(`localhost`)で起動して動作すること(`mvn spring-boot:run` 等)。
- 主要ロジック(パーサ・集計)には自動テストを書くこと。
- 解析不能行があってもアプリが落ちないこと(件数を数えて継続)。
- 過度に大きいファイルは対象外とし、まずは数千〜数万行規模を想定する。

## 7. スコープ外(将来拡張)

- 複数ログ形式の自動判別(まずは Combined Log Format のみ)
- 認証・複数ユーザー対応
- データベース永続化(MVPはメモリ上で集計)
- リアルタイム監視・アラート

## 8. 段階実装(フェーズ)

学習しながら壊さず進めるため、以下の順で段階的に実装する。前フェーズが動作確認できてから次へ進む。

| フェーズ | 内容 | 完了の目安 |
|---|---|---|
| P1 | プロジェクト雛型作成・起動確認 | Spring Bootが起動し、トップページ or ヘルスチェックが表示される |
| P2 | ログ1行をパースするロジック+テスト | Combined Log Format 1行を各項目に分解でき、テストが通る |
| P3 | 複数行の集計ロジック+テスト | ステータス別・上位パス・上位IP・時間帯別を集計でき、テストが通る |
| P4 | REST API(JSONを返す) | ログを渡すと集計結果JSONが返る |
| P5 | Web画面(アップロード+結果表示) | 画面からログを投入し、結果が表で見える |
| P6 | 仕上げ(グラフ・エラー抽出・README整備) | ポートフォリオとして見せられる状態 |

各フェーズは「1つのまとまり」ごとにGitコミットし、キリのいいところでGitHubにpushする(git-practice で学んだ流れを実践する)。

## 9. ポートフォリオとしての見せ方

- READMEに「何ができるか」「起動方法」「画面のスクリーンショット」「工夫した点(テスト・設計)」を書く。
- パーサ・集計のテストコードを見せることで、品質意識をアピールする。
- 余力があれば GitHub Actions で自動テスト(CI)を回し、バッジを付ける。
