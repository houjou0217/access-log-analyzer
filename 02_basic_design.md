# Webアクセスログ解析ツール 基本設計書

本書は「01_requirements.md(要件定義書)」を前提とする。ここではシステム構成・技術スタック・画面構成・機能一覧・概念データ構造・処理の流れを定める。クラス設計・パース正規表現・API仕様・集計アルゴリズムの詳細は「03_detailed_design.md(詳細設計書)」で扱う。

## 0. 本ドキュメントの位置づけ(AI駆動開発前提)

本設計書群は、実装を担当するAIエージェント(Claude Code等)が読み取って実装するための入力仕様として作成する。仕様は曖昧さを排し、具体値・条件・境界を明示する。確定していない点は「未確定事項」と明記する。用語・クラス名・項目名は日本語表記と英語識別子を併記する(英語識別子は詳細設計で確定)。

## 1. 技術スタック

| 区分 | 採用技術 |
|---|---|
| 言語 | Java 21 (LTS) |
| フレームワーク | Spring Boot(Spring Web) |
| ビルドツール | Maven |
| 画面 | Thymeleaf(サーバーサイドレンダリング) |
| テスト | JUnit 5 |
| IDE | IntelliJ IDEA Community |

## 2. システム全体構成

単一の Spring Boot アプリケーションとして動作する。外部DB・外部サービスには依存せず、集計はメモリ上で完結する(ローカル起動 → ブラウザからアクセス)。

### 2.1 レイヤー構成

```
[ブラウザ]
   │  ①ログ本文＋集計パラメータを送信(フォーム/API)
   ▼
[Controller層]   … 画面用(Thymeleaf)とREST API用
   │  ②入力を受け取り、Service層へ渡す
   ▼
[Service層]
   ├─ パーサ(LogParser)   … ログ1行→LogEntry
   └─ 集計器(LogAggregator) … 複数LogEntry→AnalyzeResult
   │  ③集計結果を返す
   ▼
[Controller層]
   │  ④画面(HTML)またはJSONとして返却
   ▼
[ブラウザ]  … 結果を表・グラフで表示
```

### 2.2 データの流れ

1. ブラウザの入力画面で、ログ本文をテキストエリアに貼り付け、集計パラメータ(Top N・時間バケット単位)を指定して送信する。
2. Controllerが入力を受け取り、Service層に渡す。
3. パーサが1行ずつ解析して LogEntry に変換し、解析できない行はスキップ数として数える。
4. 集計器が LogEntry 群から各種集計(AnalyzeResult)を作る。
5. 画面用Controllerは結果をThymeleafでHTML描画、REST用ControllerはJSONで返す。

## 3. 画面構成

利用者は本人想定のため認証は設けない。

### 3.1 入力画面(トップ)

- ログ本文を貼り付けるテキストエリア
- 集計パラメータの入力: Top N(上位何件を出すか)、時間バケット単位(1時間 / 10分 など)
- 「解析する」ボタン

### 3.2 結果画面

同一ページ内または遷移先で、以下を表示する。

- サマリ: 総行数 / 解析成功数 / 解析不能行数
- ステータスコード別集計(2xx/3xx/4xx/5xx の区分と主要コード個別)
- アクセス上位パス(Top N)
- アクセス上位IP(Top N)
- 時間帯別リクエスト数(指定バケット単位。表、可能なら棒グラフ)
- エラー行(4xx/5xx)の抽出一覧

## 4. 機能一覧と概念データ構造

### 4.1 概念データ

| 名称 | 内容 |
|---|---|
| ログ行(LogEntry) | パース済みの1リクエスト。IP・日時・メソッド・パス・プロトコル・ステータス・サイズ・リファラ・UA |
| 解析要求(AnalyzeRequest) | 入力ログ本文と集計パラメータ(Top N・バケット単位) |
| 解析結果(AnalyzeResult) | 各種集計結果のまとまり(4.2参照) |

### 4.2 集計結果に含まれるもの

- 総行数・解析成功数・解析不能行数
- ステータスコード別件数(区分 2xx/3xx/4xx/5xx と個別コード)
- 上位パス(パスと件数の一覧、Top N)
- 上位IP(IPと件数の一覧、Top N)
- 時間帯別件数(バケットと件数の一覧)
- エラー行一覧(4xx/5xx の LogEntry)

### 4.3 機能一覧

| 分類 | 機能 |
|---|---|
| パース | ログ1行の分解、日時の解釈、解析不能行のスキップとカウント |
| 集計 | 総数/成功/スキップ、ステータス別、上位パス、上位IP、時間帯別、エラー抽出 |
| API | 解析結果をJSONで返すREST API |
| 画面 | 入力(貼り付け+パラメータ)、結果表示(表・グラフ) |

## 5. 対象ログ形式(再掲・確定)

Apache/Nginx の Combined Log Format を対象とする。詳細なパース仕様(正規表現・各項目の型・欠損時の扱い)は 03 で定義する。形式に合わない行は処理を止めず、解析不能行としてカウントする。

## 6. 本フェーズで確定した事項・積み残し

確定事項: 技術スタック、全体構成、レイヤー構成、画面構成、機能一覧、概念データ、入力方法(テキスト貼り付け)、UI方式(Thymeleaf)、集計パラメータを画面から変更可能とすること。

詳細設計で詰める積み残し(未確定事項): クラス設計とパッケージ構成、パース正規表現と各項目の型・欠損時の扱い、集計アルゴリズムと並び順、時間バケットの選択肢と既定値、Top N の既定値と上限、REST APIのエンドポイント・リクエスト/レスポンス構造、画面の項目レイアウト、グラフ描画の実現方法、機能IDと実装フェーズの割り当て。

## 7. 今後の進め方

1. 本基本設計書を確認・合意する
2. 詳細設計フェーズへ移行する(クラス設計・パース仕様・API仕様・機能ID・フェーズ)
3. 詳細設計完了後、Claude Codeによる実装フェーズへ進む
